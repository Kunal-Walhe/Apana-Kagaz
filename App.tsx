import { initialPoetryData } from './data';

// Other imports...

const loadData = () => {
    // Use the initialPoetryData directly instead of fetching data.json
    return initialPoetryData;
};

// Rest of the App.tsx code...
